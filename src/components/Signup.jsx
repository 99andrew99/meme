import styled from "styled-components";

const TopContainer = styled.div`
    background-color: tomato;
`;

function Signup() {
    return <TopContainer>회원가입</TopContainer>;
}

export default Signup;
